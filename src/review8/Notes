Review 8:

Variables Syntax
     OPTIONAL       OPTIONAL       MUST    MUST
accessModifier nonAccess modifier dataType name;

Methods Syntax
    OPTIONAL        OPTIONAL          MUST         MUST
accessModifier nonAccess modifier returntype, methodname(){
}

Access Modifiers:
from most restrictive to less restrictive
private -> default -> protected -> public